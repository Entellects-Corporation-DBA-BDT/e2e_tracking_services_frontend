import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { FaArrowRight, FaBuilding, FaEye, FaEyeSlash, FaGoogle, FaLock, FaMicrosoft, FaShieldAlt, FaUser } from "react-icons/fa";
import { loginUser } from "../api/authApi";
import "../styles/LoginForm.css";
function LoginForm(){const [email,setEmail]=useState(()=>localStorage.getItem("rememberedEmail")||"");const [password,setPassword]=useState("");const [remember,setRemember]=useState(Boolean(localStorage.getItem("rememberedEmail")));const [loading,setLoading]=useState(false),[errorMessage,setErrorMessage]=useState(""),[showPassword,setShowPassword]=useState(false);const navigate=useNavigate();const submit=async e=>{e.preventDefault();setErrorMessage("");if(!email.trim()||!password){setErrorMessage("Please enter your username and password.");return}setLoading(true);try{const response=await loginUser({email:email.trim(),password});if(remember)localStorage.setItem("rememberedEmail",email.trim());else localStorage.removeItem("rememberedEmail");navigate(response.firstRoute,{replace:true})}catch(error){setErrorMessage(error.message||"Login failed. Please check your credentials.")}finally{setLoading(false)}};return <div className="loginPage"><div className="loginContainer"><div className="logoSection"><img src="/logo.png" alt="E2E Tracking Services"/><h1>E2E TRACKING</h1><h3>SERVICES</h3></div><div className="welcomeSection"><h2>Welcome Back!</h2><p>Login to continue to your account</p></div><form className="loginForm" onSubmit={submit}><label className="inputGroup"><span>Username</span><div className="inputWrapper"><FaUser/><input type="text" autoComplete="username" placeholder="Enter your email or username" value={email} onChange={e=>setEmail(e.target.value)}/></div></label><label className="inputGroup"><span>Password</span><div className="inputWrapper"><FaLock/><input type={showPassword?"text":"password"} autoComplete="current-password" placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShowPassword(v=>!v)} aria-label={showPassword?"Hide password":"Show password"}>{showPassword?<FaEyeSlash/>:<FaEye/>}</button></div></label><div className="optionsWrapper"><label><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> Remember me</label><button type="button" onClick={()=>setErrorMessage("Please contact your administrator to reset your password.")}>Forgot Password?</button></div><button className="loginButton" disabled={loading}>{loading?"Signing in...":"Sign In"}<FaArrowRight/></button>{errorMessage&&<p className="login-error-message" role="alert">{errorMessage}</p>}</form><div className="dividerWrapper"><i/><span>or sign in with</span><i/></div><div className="socialWrapper"><button disabled title="Google sign-in is not configured"><FaGoogle/></button><button disabled title="Microsoft sign-in is not configured"><FaMicrosoft/></button><button disabled title="SSO is not configured"><FaShieldAlt/> SSO</button></div><button className="register-link" onClick={()=>navigate('/register')}>New user registration</button><div className="securityContainer"><span><FaLock/> SSL Secured</span><span><FaShieldAlt/> 99.9% Uptime</span><span><FaBuilding/> Enterprise Ready</span></div><footer><p>Version 2.1.0</p><small>© 2026 Bee Data Technologies. All rights reserved.</small></footer></div></div>}
export default LoginForm;